name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read
  pull-requests: write

env:
  CARGO_TERM_COLOR: always

jobs:
  test:
    name: Test & Profile
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: dtolnay/rust-toolchain@stable

      - uses: Swatinem/rust-cache@v2

      # Run unit tests for all workspace crates.  The panic-contract
      # fixture intentionally panics in one test — this step reports
      # that failure (expected) but does not block CI.
      - name: Run workspace unit tests
        run: cargo test --workspace --lib
        continue-on-error: true

      # CLI integration tests exercise the profiler against four fixture
      # contracts: happy path, panicking test, duplicate labels, and
      # compile errors.  These run `cargo run -p cli` as a subprocess
      # and verify the profiler handles each scenario correctly.
      - name: Run CLI integration tests
        run: cargo test -p soroban-cost-cli --test integration_test

      - name: Run cost profiler against example contract
        id: profile
        run: |
          report=$(cargo run -p soroban-cost-cli -- report --manifest-path examples/token-example --html cost-report.html 2>&1)
          echo "$report"
          # Use EOF delimiter to handle multi-line output safely
          {
            echo 'report<<PROFILER_EOF'
            echo "$report"
            echo 'PROFILER_EOF'
          } >> "$GITHUB_OUTPUT"

      - name: Upload HTML report
        uses: actions/upload-artifact@v4
        with:
          name: cost-report
          path: cost-report.html
          retention-days: 30

      # Post the cost report as a PR comment (PRs only).
      # Uses a comment-tag so repeated pushes update the same comment.
      - name: Post report to PR
        if: github.event_name == 'pull_request'
        uses: peter-evans/create-or-update-comment@v4
        with:
          comment-tag: soroban-profiler-report
          body: |
            ## Soroban Cost Report

            ```
            ${{ steps.profile.outputs.report }}
            ```

            [Full HTML report](${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}) available as a build artifact.
        continue-on-error: true
